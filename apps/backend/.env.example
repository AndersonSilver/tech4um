NODE_ENV=development
PORT=3333

# OBRIGATÓRIO: gere um valor aleatório forte, nunca reutilize este exemplo.
# Sugestão: openssl rand -hex 32
JWT_SECRET=GERE_UM_VALOR_ALEATORIO_DE_NO_MINIMO_32_CARACTERES
JWT_EXPIRES_IN=2h

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=tech4um

# OBRIGATÓRIO em produção (sem fallback para "*")
CORS_ORIGIN=http://localhost:5173

GOOGLE_CLIENT_ID=seu_client_id.apps.googleusercontent.com
